<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data />

    <com.genesys.shape.layout.ShapeConstraintLayout
        style="@style/Widget.Codebase.Panel.MediaCard">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/img_thumbnail"
            style="@style/Widget.Codebase.Surface.MediaPlaceholder"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@tools:sample/backgrounds/scenic" />

        <ImageView
            android:id="@+id/iv_premium_badge"
            android:layout_width="@dimen/ds_size_badge_sm"
            android:layout_height="@dimen/ds_size_badge_sm"
            android:layout_margin="@dimen/ds_space_xxs"
            android:contentDescription="@string/template_premium_badge"
            android:src="@drawable/ic_premium_badge"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/tv_template_name"
            style="@style/Widget.Codebase.Scrim.Label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            tools:text="Anime Style" />

    </com.genesys.shape.layout.ShapeConstraintLayout>
</layout>
